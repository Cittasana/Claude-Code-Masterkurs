---
phase: 01-design-system-foundation
plan: 01
type: execute
depends_on: []
files_modified: [claude-code-masterkurs/tailwind.config.js, claude-code-masterkurs/src/index.css]
---

<objective>
Establish the Apple-inspired grey and orange color palette as the foundation for the entire redesign.

Purpose: Replace the GitHub dark theme colors with a premium grey/orange palette that creates visual distinction and brand identity.
Output: Updated Tailwind config with new color tokens, CSS custom properties aligned with the design system.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md

**Key files:**
@claude-code-masterkurs/tailwind.config.js
@claude-code-masterkurs/src/index.css

**Design Direction (from PROJECT.md):**
- Apple design language: clean, minimal, confident
- Color palette: Grey tones (backgrounds, text) + Orange accents (CTAs, highlights)
- Current state: GitHub dark theme colors (#0d1117, #161b22, #30363d, #1f6feb)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Apple-inspired grey/orange color palette in Tailwind</name>
  <files>claude-code-masterkurs/tailwind.config.js</files>
  <action>
Replace the `github` color namespace with a new `apple` namespace containing:

**Grey scale (backgrounds and text):**
- `bg`: #1a1a1a (deep charcoal - main background)
- `surface`: #2d2d2d (elevated surfaces, cards)
- `elevated`: #3d3d3d (modals, dropdowns)
- `border`: #404040 (subtle borders)
- `muted`: #8e8e93 (secondary text, placeholders)
- `text`: #f5f5f7 (primary text - Apple's signature off-white)
- `textSecondary`: #a1a1a6 (secondary text)

**Orange accent scale:**
- `accent`: #ff9500 (Apple's signature orange - primary CTAs)
- `accentHover`: #ffaa33 (hover state)
- `accentMuted`: #cc7700 (pressed/active state)
- `accentSubtle`: rgba(255, 149, 0, 0.15) (subtle backgrounds)

**Semantic colors:**
- `success`: #30d158 (Apple green)
- `error`: #ff453a (Apple red)
- `warning`: #ffd60a (Apple yellow)

Keep darkMode: 'class' configuration. Remove the old `github` namespace entirely.
  </action>
  <verify>Run `cd claude-code-masterkurs && npm run build` - should complete without errors</verify>
  <done>tailwind.config.js has new `apple` color namespace with grey scale, orange accents, and semantic colors. No `github` colors remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update CSS custom properties to match new palette</name>
  <files>claude-code-masterkurs/src/index.css</files>
  <action>
In the `:root` block within `@layer base`, replace existing CSS custom properties:

```css
:root {
  /* Background scale */
  --color-bg: #1a1a1a;
  --color-surface: #2d2d2d;
  --color-elevated: #3d3d3d;
  --color-border: #404040;

  /* Text scale */
  --color-text: #f5f5f7;
  --color-text-secondary: #a1a1a6;
  --color-muted: #8e8e93;

  /* Accent */
  --color-accent: #ff9500;
  --color-accent-hover: #ffaa33;
  --color-accent-muted: #cc7700;

  /* Semantic */
  --color-success: #30d158;
  --color-error: #ff453a;
  --color-warning: #ffd60a;
}
```

These CSS custom properties allow for easy theming and can be used directly in CSS when Tailwind classes aren't convenient.
  </action>
  <verify>CSS file parses correctly - no syntax errors when running build</verify>
  <done>CSS custom properties updated with new color values matching Tailwind config</done>
</task>

<task type="auto">
  <name>Task 3: Update base body and code styles to use new colors</name>
  <files>claude-code-masterkurs/src/index.css</files>
  <action>
Update the `body` rule in `@layer base` to use the new color classes:

```css
body {
  @apply bg-apple-bg text-apple-text antialiased;
  font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'SF Pro Text', 'Segoe UI', 'Noto Sans', Helvetica, Arial, sans-serif;
  margin: 0;
  min-height: 100vh;
}
```

Note: Added 'SF Pro Display' and 'SF Pro Text' to the font stack for users who have these fonts installed. Falls back gracefully to system fonts.

Update the Prism.js theme section at the bottom to use new colors:

```css
/* Prism.js Dark Theme - Apple Style */
pre[class*="language-"] {
  @apply bg-apple-surface border border-apple-border rounded-xl;
}

code[class*="language-"] {
  @apply text-apple-text;
}

.token.comment {
  @apply text-apple-muted;
}

.token.keyword {
  color: #ff79c6; /* Pink - good contrast on dark */
}

.token.string {
  color: #8be9fd; /* Cyan - readable */
}

.token.function {
  color: #bd93f9; /* Purple */
}

.token.number {
  color: #ff9500; /* Orange accent */
}
```
  </action>
  <verify>Run `cd claude-code-masterkurs && npm run dev` and visually confirm the app loads with new dark grey background instead of GitHub dark blue</verify>
  <done>Body uses apple-bg and apple-text classes. Prism theme updated. App displays with new color foundation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes (or only pre-existing warnings)
- [ ] No references to `github-*` color classes remain in index.css
- [ ] App loads with charcoal grey background (#1a1a1a) instead of GitHub blue-black (#0d1117)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Color palette established as foundation for subsequent plans
- No visual regressions (app still loads and displays content)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-01-SUMMARY.md`
</output>
