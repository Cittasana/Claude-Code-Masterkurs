# Email-Positioning — Track: codex

> Pro-Track Tonality-Layer für `masterkurs-email-sequence`.
> Wird vom Skill geladen wenn `--track codex` aktiv ist.
> **Sprach-Hinweis**: EN ist Source-of-Truth für diesen Track (Locked Decision #5),
> DE-Variante über LLM-Translation + Founder-Review.

## Subject-Line Voice

**Tonality**: enterprise-confident, GPT-5.5-class, professionell
**Hook-Pattern**: Vergleich mit Konkurrenz (Cursor, Copilot CLI, Claude Code) erlaubt; konkrete Modell-/Tool-Namen
**Emoji-Politik**: 0–1 — Enterprise-Käuferschicht, eher zurückhaltend

**Beispiele**:
- "Codex CLI 0.130: AGENTS.md is the new CLAUDE.md"
- "Why GPT-5.5 in Codex Cloud beats local Cursor on long-context refactors"
- "Codex JetBrains plugin: 5 settings you should change today"

## Opener Style

Daten-getrieben, mit Benchmark-Referenz oder Tool-Version. Englischer Tonfall: präzise, kein Marketing-Floskel-Pad.

**Beispiel-Opener 1 (EN)**:
> Codex CLI 0.130 shipped two weeks ago and quietly changed how AGENTS.md gets
> resolved across nested workspaces. If you were leaning on the old behavior,
> here's the fix — plus a 90-second migration script.

**Beispiel-Opener 2 (DE-Variante)**:
> Codex Cloud rechnet seit März die Long-Context-Refactors anders ab.
> In Lektion 6 zeige ich dir, wie du den Cost-Per-Run um 60% drückst,
> ohne Output-Qualität zu verlieren.

## CTA Tone

Direkt, business-orientiert. "Open lesson" / "Lektion starten" — kein "Hey check this out".

**CTA-Beispiele (EN/DE-parallel)**:
- `Open lesson →` / `Lektion starten →`
- `View benchmark data →` / `Benchmark-Daten ansehen →`
- `Copy AGENTS.md template →` / `AGENTS.md-Template kopieren →`
- `Run the migration script →` / `Migrations-Script ausführen →`

## 3 Key Value-Prop Bullets

1. **Codex across all surfaces** — CLI, Cloud, JetBrains, ChatGPT integration. One curriculum, four interfaces, GPT-5.5-class throughout. (DE: "Codex auf allen Oberflächen — CLI, Cloud, JetBrains, ChatGPT-Integration. Ein Curriculum, vier Interfaces.")
2. **AGENTS.md as the spec** — track-spezifischer Fokus auf den OpenAI-Spec-Standard (vs. CLAUDE.md im Claude-Code-Track), Multi-Agent-Tree-Patterns, Codex-CLI-Hooks.
3. **EN-First, DE-mitgeliefert** — internationaler Tech-Persona im Original, deutsche Übersetzung für DACH-Käufer:innen kostenlos dabei. `hreflang`-correct, kein i18n-Aufpreis.
